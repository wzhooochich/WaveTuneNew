<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WaveTuneNew.GenrePage"
             BackgroundColor="#121212">

    <Grid RowDefinitions="60, *">

        <Border Grid.Row="0" BackgroundColor="#181818" StrokeThickness="0">
            <Grid ColumnDefinitions="Auto, *" Padding="20,0" ColumnSpacing="10">
                <ImageButton Grid.Column="0"
                             Source="back.png"
                             HeightRequest="20"
                             WidthRequest="20"
                             BackgroundColor="Transparent"
                             Command="{Binding GoBackCommand}"
                             ToolTipProperties.Text="Назад" />
                <Label Grid.Column="1"
                       Text="{Binding GenreName}"
                       TextColor="White"
                       FontSize="16"
                       FontAttributes="Bold"
                       VerticalOptions="Center"
                       CharacterSpacing="2" />
            </Grid>
        </Border>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Songs}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="20,10"
                          ColumnDefinitions="Auto, *"
                          ColumnSpacing="15">
                        <Border Grid.Column="0"
                                StrokeShape="RoundRectangle 6"
                                StrokeThickness="0">
                            <Image Source="{Binding PictureUrl}"
                                   WidthRequest="50"
                                   HeightRequest="50"
                                   Aspect="AspectFill" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                            <Label Text="{Binding Title}"
                                   TextColor="White"
                                   FontSize="15"
                                   FontAttributes="Bold" />
                            <Label Text="{Binding Author}"
                                   TextColor="#888888"
                                   FontSize="12" />
                        </VerticalStackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
